function [x,y] = correctDimensions(xc,yc)

% Himanshu Dahiya(201330046)
% Sanatan Mishra (201330025)

%well we could have done this in the script itself :P
    x=min(xc);
    y=min(yc);

end

